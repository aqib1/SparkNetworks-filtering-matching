spring:
  application:
    name: spark-networks-backend
server:
  port: 8082
  
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8085/eureka}
  instance:
    preferIpAddress: true    